# name: test/sql/odbc_platform.test
# description: test odbc extension
# group: [odbc]
require notwindows

# Require statement will ensure this test is run with this extension loaded
require odbc

statement ok
from glob('*');

statement ok
from glob('/duckdb_build_dir/**');

# First try using a relative path that should work in most environments
query I
FROM odbc_query('Driver=/workspaces/duckdb-odbc-extension/test/drivers/libduckdb_odbc.so;Database=:memory:', 'SELECT 42')
----
42

query II
from odbc_drivers();
----
